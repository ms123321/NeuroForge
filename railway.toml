[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Git-controlled start (clear any old "gunicorn ..." in the Railway UI
# or this may be overridden by a saved Custom Start Command)
startCommand = "python start.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
